import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  flatListWrapper: {
    height: 280,
  },
  titleList: {
    color: '#000',
    fontSize: 30,
    fontWeight: 'bold',
    marginLeft: 15,
    marginBottom: 10,
  },
});
