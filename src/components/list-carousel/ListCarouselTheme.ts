import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  flatListWrapper: {
    backgroundColor: 'red',
    height: 250,
  },
  titleList: {
    fontSize: 30,
    fontWeight: 'bold',
  },
});
